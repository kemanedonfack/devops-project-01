pipeline {
    agent {
        docker {
            image 'mysql:5.7'
            args '--name=mysqldb -p 3306:3306 -e mysql_root_password=kemane mysql_database=db_zencaisse'
        }
    }
    stages {
        stage('Test') { 
            steps {
                sh 'mysql --version' 
            }
        }
    }
}
